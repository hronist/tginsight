# Environment variables (optional)
#
# API keys for LLM live in the browser (Settings → localStorage).
# This file is only for optional server-side / deploy knobs.
#
# NODE_ENV=development

# No required secrets for local `npm run dev`.
# Proxy mode (/api/chat) expects the user API key in request headers, not here.
